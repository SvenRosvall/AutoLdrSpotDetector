#include "Streaming.h"

Print & operator<<(Print & p, const char * s)
{
  return p;
}

Print & operator<<(Print & p, int i)
{
  return p;
}

Print & operator<<(Print & p, float f)
{
  return p;
}
int endl;
